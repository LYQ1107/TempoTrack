# TempoTrack research data contract

Frozen appearance features are external inputs and are never committed to the
repository.  `inventory` records their local path, split overlap, and hashes
when feasible.  `prepare` converts accepted inputs to safe NPZ observation
ledgers; labels remain separate annotation artifacts.
