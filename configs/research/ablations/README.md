# Planned ablations

The complete matrix is recorded in `suite.yaml`.  S1-specific ablations are
registered in the execution report only after they have real artifacts:
ordinary metric, prediction-only, prediction+identity, no regularizer,
history shuffle, source replacement, forward-only, bidirectional inpainting,
and chain refinement.
